# This file contains utility functions if needed in the future.
# Currently empty, but can add helpers like logging or chunking for large files.

def log_message(message):
    print(f"[LOG] {message}")

# Example usage: log_message("Operation started")